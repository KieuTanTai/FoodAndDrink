// Định nghĩa các giá trị timeout cụ thể, dùng cho toàn dự án React TypeScript

// Timeout cho form (ví dụ: thời gian chờ validation, submit, v.v.)
export const FORM_TIMEOUT = 10_000; // 10 giây

// Timeout cho các request HTTP/API
export const REQUEST_TIMEOUT = 15_000; // 15 giây

// Timeout cho thông báo/toast message
export const TOAST_TIMEOUT = 3_000; // 3 giây

// Có thể bổ sung thêm các giá trị timeout khác nếu cần